using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceType ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class IncidenceType_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceType.IncidenceType())
            {
                screen.NavigateToScreen<IncidenceTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class IncidenceType_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceType.IncidenceType())
            {
                screen.NavigateToScreen<IncidenceTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class IncidenceType_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceType.IncidenceType())
            {
                screen.NavigateToScreen<IncidenceTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}