using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Order ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class Order_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Order.Order())
            {
                // screen.NavigateToScreen<OrderCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class Order_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Order.Order())
            {
                // screen.NavigateToScreen<OrderCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class Order_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Order.Order())
            {
                // screen.NavigateToScreen<OrderCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}