using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.OrderSituation ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class OrderSituation_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.OrderSituation.OrderSituation())
            {
                screen.NavigateToScreen<OrderSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class OrderSituation_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.OrderSituation.OrderSituation())
            {
                screen.NavigateToScreen<OrderSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class OrderSituation_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.OrderSituation.OrderSituation())
            {
                screen.NavigateToScreen<OrderSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}