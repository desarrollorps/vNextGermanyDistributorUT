using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceSituation ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class IncidenceSituation_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceSituation.IncidenceSituation())
            {
                screen.NavigateToScreen<IncidenceSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class IncidenceSituation_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceSituation.IncidenceSituation())
            {
                screen.NavigateToScreen<IncidenceSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class IncidenceSituation_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.IncidenceSituation.IncidenceSituation())
            {
                screen.NavigateToScreen<IncidenceSituationCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}