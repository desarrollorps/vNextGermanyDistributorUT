using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.ReturnCause ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class ReturnCause_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.ReturnCause.ReturnCause())
            {
                screen.NavigateToScreen<ReturnCauseCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class ReturnCause_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.ReturnCause.ReturnCause())
            {
                screen.NavigateToScreen<ReturnCauseCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class ReturnCause_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.ReturnCause.ReturnCause())
            {
                screen.NavigateToScreen<ReturnCauseCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}