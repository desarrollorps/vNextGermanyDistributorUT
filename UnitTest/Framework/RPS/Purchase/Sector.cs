using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Sector ;
namespace UnitTest.Framework.RPS.Services.Purchase
{
    public class Sector_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Sector.Sector())
            {
                // screen.NavigateToScreen<SectorCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class Sector_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Sector.Sector())
            {
                // screen.NavigateToScreen<SectorCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class Sector_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Purchase.Sector.Sector())
            {
                // screen.NavigateToScreen<SectorCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}