using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Incident ;
namespace UnitTest.Framework.RPS.Services.BPM
{
    public class Incident_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Incident.Incident())
            {
                // screen.NavigateToScreen<IncidentCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class Incident_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Incident.Incident())
            {
                // screen.NavigateToScreen<IncidentCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class Incident_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Incident.Incident())
            {
                // screen.NavigateToScreen<IncidentCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}