using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.BPM.IncidentType ;
namespace UnitTest.Framework.RPS.Services.BPM
{
    public class IncidentType_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.IncidentType.IncidentType())
            {
                screen.NavigateToScreen<IncidentTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class IncidentType_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.IncidentType.IncidentType())
            {
                screen.NavigateToScreen<IncidentTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class IncidentType_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.IncidentType.IncidentType())
            {
                screen.NavigateToScreen<IncidentTypeCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}