using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Stakeholder ;
namespace UnitTest.Framework.RPS.Services.BPM
{
    public class Stakeholder_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Stakeholder.Stakeholder())
            {
                // screen.NavigateToScreen<StakeholderCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class Stakeholder_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Stakeholder.Stakeholder())
            {
                // screen.NavigateToScreen<StakeholderCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class Stakeholder_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.BPM.Stakeholder.Stakeholder())
            {
                // screen.NavigateToScreen<StakeholderCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}