using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Manufacturing.StructureType ;
namespace UnitTest.Framework.RPS.Services.Manufacturing
{
    public class StructureType_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Manufacturing.StructureType.StructureType())
            {
                // screen.NavigateToScreen<StructureTypeCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class StructureType_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Manufacturing.StructureType.StructureType())
            {
                // screen.NavigateToScreen<StructureTypeCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class StructureType_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Manufacturing.StructureType.StructureType())
            {
                // screen.NavigateToScreen<StructureTypeCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}