using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Maintenance.Terminal ;
namespace UnitTest.Framework.RPS.Services.Maintenance
{
    public class Terminal_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Maintenance.Terminal.Terminal())
            {
                // screen.NavigateToScreen<TerminalCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class Terminal_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Maintenance.Terminal.Terminal())
            {
                // screen.NavigateToScreen<TerminalCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class Terminal_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Maintenance.Terminal.Terminal())
            {
                // screen.NavigateToScreen<TerminalCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}