using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Quality.ControlPatternVersion ;
namespace UnitTest.Framework.RPS.Services.Quality
{
    public class ControlPatternVersion_New
    {
        [Fact]        
        public void New_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.ControlPatternVersion.ControlPatternVersion())
            {
                // screen.NavigateToScreen<ControlPatternVersionCollectionView>();
                throw new NotImplementedException();
            }
            
        }
    }
    public class ControlPatternVersion_Update
    {
        [Fact]        
        public void Update_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.ControlPatternVersion.ControlPatternVersion())
            {
                // screen.NavigateToScreen<ControlPatternVersionCollectionView>();
                throw new NotImplementedException();
            }

        }
    }
    public class ControlPatternVersion_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.ControlPatternVersion.ControlPatternVersion())
            {
                // screen.NavigateToScreen<ControlPatternVersionCollectionView>();
                throw new NotImplementedException();
            }
        }
    }
}