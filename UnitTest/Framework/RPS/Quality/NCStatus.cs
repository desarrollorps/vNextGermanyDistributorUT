using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCStatus ;
namespace UnitTest.Framework.RPS.Services.Quality
{
    public class NCStatus_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCStatus.NCStatus())
            {
                screen.NavigateToScreen<NCStatusCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class NCStatus_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCStatus.NCStatus())
            {
                screen.NavigateToScreen<NCStatusCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class NCStatus_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCStatus.NCStatus())
            {
                screen.NavigateToScreen<NCStatusCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}