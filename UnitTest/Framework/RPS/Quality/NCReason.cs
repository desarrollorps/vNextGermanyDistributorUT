using vNextGermanyDistributorUT;
using Xunit;
using System;
using SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCReason ;
namespace UnitTest.Framework.RPS.Services.Quality
{
    public class NCReason_New
    {
        [Fact]        
        public void New_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCReason.NCReason())
            {
                screen.NavigateToScreen<NCReasonCollectionView>();
                
            }
            */
            throw new NotImplementedException();
            
        }
    }
    public class NCReason_Update
    {
        [Fact]        
        public void Update_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCReason.NCReason())
            {
                screen.NavigateToScreen<NCReasonCollectionView>();
                
            }
            */
            throw new NotImplementedException();

        }
    }
    public class NCReason_Delete
    {
        [Fact]        
        public void Delete_UT()
        {
            /*
            var config = SeleniumConfig.Current;
            using (var screen = new SeleniumGeneratedClasses.Framework.RPS.Services.Quality.NCReason.NCReason())
            {
                screen.NavigateToScreen<NCReasonCollectionView>();
                
            }
            */
            throw new NotImplementedException();
        }
    }
}